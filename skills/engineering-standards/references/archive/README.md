# Archive — historical originals

These are the original forms of the team guidelines, **superseded on 2026-09-16** by the Markdown in `../` (`backend.md`, `frontend.md`, `database.md`, `java-code-review.md`, plus the cross-stack rules in `../../SKILL.md`), which is now the source of truth and is edited directly.

- `SIS Development Guidelines Documentation.docx` — the team's development guidelines. Its 14 screenshots were exported to `../images/` and the rules they showed were written out in the Markdown.
- `java-code review guideline/` — the six Java review PDFs. They are text only (checked for embedded figures; there are none), so nothing was lost.

Kept only so nothing is lost. **Do not edit them, and do not treat them as current** — they are frozen at the 2026-09-16 content while the Markdown moves on. Agents never read them: Claude Code cannot read a .docx, and reading a PDF costs far more than the Markdown.
