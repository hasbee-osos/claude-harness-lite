<!-- QA packet: pasted by a developer as a Jira comment on a Bug. Rules in skills/input-packets. Delete this line and every <placeholder>. -->
**Dev analysis for QA: <one line: what we need from you, e.g. "retest needed before any fix">**

**Summary**
<2–4 sentences in plain language: what we found, whether the cause is known, and what has changed since the bug was reported (e.g. a related fix under GSIS-123 already deployed to <env>). No code, no commit hashes.>

**Retest steps** (on <environment(s)>)
1. <role> — <screen / action>
2. <…>
3. After each action, check <what, and for whom>.

<Optional, one short paragraph: anything that could mislead the retest, e.g. "check email first — in-app is unreliable for applicants on every event".>

**Please reply with**
1. **<Result to report>** (environment + date/time of the test):
   * <case>: <observable> Y/N · <observable> Y/N
2. **<Question about expected behaviour>** — <one clause on why it matters>
   * (a) <option>
   * (b) <option>
   * (c) other: ______
3. **<Yes/no question>** — <why>. Y/N

<If the result still differs from expected: what extra evidence to attach — exact time of the action, screenshot or recording, the account role used.>
